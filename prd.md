1. 전체 아키텍처 및 기술 스택

서버
Node.js 기반 API 서버 + 소켓 실시간 처리 (Socket.IO)
Next.js 기반 프론트엔드 SPA (SSR 기능 일부 활용, 반응형 지원)
AWS Elastic Beanstalk으로 배포

DB
Firebase FireStore
방 데이터, 문제/ 정답/ 참가자/점수 등 저장 
방 종료 시 해당 데이터 삭제(영구 저장 X. 학습/보안 부담 최소화)+ 해당 데이터 문제와 정답을 남겨서 추후 다른 호스트가 이용하도록 하는 기능은 나중에 구현
 
실시간 통신
Socket.IO 채널 활용
문제 출제/답안 제출/점수 집계/탈락/아바타 변경 등 이벤트 실시간 송수신


2. 사용자 역할 및 화면 설계
메인 화면에서 방 만들기, 참여하기 탭 버튼을 클릭할 수 있음.
방만들기 탭버튼을 누른 뒤 주제(10자 이내) 입력하고 '골든벨 방 만들기' 버튼을 누를 경우 교사는 문제 입력/관리 화면(관리 페이지로 통칭)으로 넘어감. 이 페이지의 좌측 상단에 방의 코드가 나옮(4자리의 영어대문자+숫자)이 탭에는 공개방으로 만들기 체크란이 있음.  (+ ai api 입력하여 gemini 인공지능으로 여러 문제 만들기는 추후에 구현.)
참여하기 탭에서 닉네임 입력, 코드 입력, 참여하기 버튼을 누르면 아바타 꾸미기 및 팀 선택 페이지로 입장함. 탭의 하단에는 만들어진 방 목록이 나옴. 주제, 공개/비공개 여부가 표시되는데 공개방을 클릭하게 될 경우 방 코드 입력란에 자동으로 코드가 채워짐. 비공개방은 코드 입력란에본인이 직접 입력해야 함. 

-관리 페이지
만들어진 방의 코드 표시
문제 및 정답 등록, 수정, 삭제, 제출, 타이머 설정(기본값 5초), 정답 공개 시간(기본값 5초) 설정함. 탈락 모드 체크란 및 탈락 조건(회) 지정 입력란 있음. 여기 기입하는 횟수 이상 틀리면 탈락처리됨.
문제 만들기를 누르면 문제 만드는 모달창이 뜸. 문제 유형은 세가지이며 모달창 안의 탭을 클릭하여 문제 유형을 정하고(ox, 객관식, 주관식) 문제를 입력할 수 있음. (추후 문제 만들때 이미지도 문제에 포함 시킬 수 있도록 구현)객관식의 경우 보기를 원하는만큼 추가할 수 있으며 정답 체크를 통해서 어떤 보기가 정답인지를 설정할 수 있음. 그리고 만드는 문제의 점수 지정할 수 있음.(default 값: 10점) 그러고 나서 '만들기' 버튼을 누르면 만든 문제가 박스가 되어 관리페이지의 왼쪽에서 아래부터 스택으로 쌓이고(박스에 표시되는 것은 '문제n: 질문' 박스 크기에 맞게 질문이 잘려서 표시됨) 순서대로 제시됨. 스택이 비어있을 경우에는 진행페이지가 대기상태가 됨. 스택의 상단 하단은 자석처럼 되서 스택을 드래그 해서 옮기면 자동으로 쌓인 박스 모양을 유지하며 상하로 옮길 수 있음(예를들어 문제1번 박스를 드래그해서 위로 올리면 자동으로 문제 2와 문제 3사이가 벌려지고 문제 2가 아래로 이동하고 문제 1이 그 사이에 위치함.), 아니면 삭제 버튼을 클릭해서 해당 스택 문제를 삭제할 수도 있음. 스택을 누르고 드래그 하여 스택의 위치를 이동시키는 방식으로 제출되는 문제의 순서를 변경할 수 있음.
페이지 오른쪽 하단에 시작&중단/ 문제 제출/ 정답 공개 버튼 이 있음. 시작버튼을 누르면 스택에서 아직 제출되지 않은 문제부터 문제가 제출되기 시작함. 중단 버튼을 누를 경우 중단됨. 이 버튼을 누르면 카운트가 강제로 2초로 바뀌어 종료되고 1초 뒤에 정답이 공개됨. 
정답 공개 버튼 옆에 '문제 제출' 버튼이 있음. 이 버튼을 누르면 1초 뒤에 강제로 다음문제로 넘어감. 이전 문제가 타이머 중이었어도 취소되고 다음 문제로 넘어감. 
진행화면의 자동 혹은 수동 모드를 설정할 수 있음. 자동의 경우는 타이머 설정에 맞게 알아서 진행됨. 수동의 경우는 정답 공개 시간이  넘어가더라도 교사가 '문제 제출'을 눌러야 다음문제로 이동함.
'순위' 버튼을 누르면 현재의 순위표가 표시됨 제일 왼쪽에는 순위, 다음은 이름, 그 다음은 점수, 그다음에는 팀 점수가 표 형식으로 제공됨.
관리 페이지에서 '진행페이지 열기' 버튼을 누르면 진행 페이지가 새 탭으로 열림. 진행 페이지는 관리 페이지와 다른 이름/파비콘('진행 페이지'/티비 아이콘 등)
종료를 누르면 점수가 높은 순서대로 순위표 제공

- 진행 페이지(관리 페이지와 동기화 되어야 함.)
진행 페이지 상단에 관리 페이지에서 제출한 문제 순차적으로 제시
타이머는 화면 우측 상단에 표시, 타이머 설정 시간으로 카운트 다운 후 1초 뒤에 정답 공개, 탈락이 안 된 학생들 답안, 정답 여부 체크
화면 하단에 입장한 학생들 전체 아바타 표시, 아바타는 속해있는 팀 알파벳 순서대로, 팀 내에서는 닉네임 오름차 순으로 정렬됨. 개인으로 참여할 경우 뒤에 정렬됨. 아바타 옆에는 그동안 쌓은 점수 표시
학생들 아바타는 이미 구성한 대로 dice avataaar2를  이용하여 커스터마이징함.
탈락 모드와 일반 모드가 있음. 탈락 모드는 탈락 조건에 지정된 숫자 이상 틀리면 탈락됨.
참가자·탈락자 관리(탈락자는 다음 문제 제출부터 회색조로 바뀜), 점수 집계/순위 표시.
탈락모드일 때 진행 화면 우측 하단에 탈락시키기와 부활시키기 버튼이 활성화 됨. 탈락시키기 버튼을 누르면 마우스 커서가 x모양 아이콘으로 바뀌고 살아있는 아바타를 누르면 그 아바타가 탈락함. 아바타가 아닌 곳을 클릭하면 커서가 바뀌고 버튼이 비활성화 됨. 마찬가지로 부활시키기 버튼을 누르고 나면 마우스 포인터가 o모양 아이콘으로 바뀌고 탈락한 아바타를 선택하면 부활됨. 이 역시 아닌 곳을 화면을 클릭할 경우 버튼이 비활성화됨.

- 학생 화면(관리 페이지와 동기화 되어야 함.)
메인 화면에서 참여하기 탭 클릭
닉네임 입력, 코드 입력 후 아바타 꾸미기 및 팀 선택 페이지 입장
팀 선택하기 버튼을 누르면 A~Z까지의 버튼이 보임. 고르면 그 알파벳에 해당하는 팀에 속하게 됨. 팀을 선택하지 않을 경우 개인으로 플레이 하는 것이 됨.
아바타에서 주어진 보기들 중에서 선택하여 아바타를 꾸미고 확인을 누르면 문제풀이 화면으로 넘어감. 

- 문제 풀이 화면
문제 시작 전이나 중에 대기 중일 때는 문제제시 구역에 '대기중'으로 표시됨.
진행 페이지의 문제, 타이머(오른쪽 상단)가 학생 화면에서도 표시됨
하단에는 답안 입력란이 있음.(객관식이나 ox의 경우 보기 버튼들 중에 클릭, 주관식의 경우 정답 입력) 제출 버튼 클릭. 클릭하면 제출버튼이 '제출완료'로 바뀌면서 비활성화됨. 그리고 이때 제출완료버튼 옆에 취소 버튼이 생김. 취소 버튼을 누르면 제출이 취소되고 제출 버튼이 활성화되어 다시 제출할 수 있음.(물론 제출완료버튼은 다시 안 보이게 됨.)
카운트 다운이 다 될 동안 제출하지 못하면 미제출이 됨. 
탈락 시 문제는 똑같이 보이지만 답안 제출이 불가능함.

- 순위표 페이지
점수 집계는 팀점수와 개인 점수 모두 집계하는데 팀 점수의 경우 개인 점수의 합계임
그룹 순서대로 표시됨. 순위의 기준은 팀 점수임. 개인으로 참여한 경우 자신의 점수가 곧 팀 점수가 됨.


3. 핵심 기능
실시간 문제 출제
실시간 답안 수집·검증
학생별 답안 입력, 자동 채점/탈락 처리
채점·점수 집계/순위
점수 자동 집계, 탈락자 점수 미적용/고정, 전체 순위판/개별 점수
탈락 및 복귀 관리
탈락 모드 활성화/비활성화, 출제자 권한으로 탈락자 복귀 가능 옵션

4. 운영 및 편의 사항
가입·인증 없이 쉽게 입장
간이 코드를 활용해 학생이 쉽고 빠르게 참가
반응형 웹앱
PC·태블릿·스마트폰 모두 호환, 별도 앱 설치 불필요
닉네임 참여
문제, 정답 공개에서 효과음 이용
게임이 종료되면 데이터 정리

5. 기술/운영 조건
항목	내용
최대 동시 접속자: 방당 약 30명, 최대 40명 정도 예상
DB 영구 저장: 없음. 방 종료 시 해당 방과 관련된 데이터는 모두 삭제
배포/운영: AWS Elastic Beanstalk +firestore
특별 기능: 문제 세트 저장이나 다운로드 기능은 현재 구현하지 않음
기본 효과음: 제공하며, 사용자가 직접 업로드하는 기능은 없음

6. 현장 차별화 포인트
완전 무료, 무가입, 링크/코드로 초간편 입장
탈락자 실시간 시각화, 자동 채점, 진행·순위 UI 분리
교사용 관리화면/진행화면 분리로 투모니터 수업 최적화
현장/실무(학교) 중심의 쉬운 관리와 몰입도 높은 학생 UI

예시 유저 흐름(학교 골든벨)
교사가 웹앱에 접속해 “새 방 생성” → 문제 입력
학생들에겐 “참가 코드/링크” 제공 → 각자 이름/닉네임으로 입장
교사는 관리 페이지 이용, 별도 모니터엔 문제 제시 및 아바타 목록 화면 띄움.
문제 제시/학생 답 입력/채점 및 탈락 처리/순위 집계

7. 윈도우에서 커서로 제작하기 때문에 백슬레시 문제, 연산자 문제, power shell 사용에 유의하여 오류가 나는 일이 없도록 할 것. 버전으로 인한 문제가 있을 경우에 context 7 mcp 활용용. 제작을 위해 더 구체화 해야할 내용이 있으면 물어보고 확인하여 진행할 것.